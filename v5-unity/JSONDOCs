JSON DOCUMENTATION

First level:
    - code contains a string with the whole source code.
    - trace contains an array for the execution trace.

An element of trace:
    - line = line number to highlight
    - stack_to_render contains an array for a stack?
    - heap maps "1", "2", "3", etc. to a heap array:
        ["FUNCTION", followed by function signature and then null
        |"LIST" followed by list elements, 
        ]
    - event = "step_line"|"call"|"return"
    - func_name = "<module>"|current function name
    - globals
    - ordered_globals
    - stdout

An element of stack_to_render:
    - encoded_locals = map variables to either the raw value, or an array of ["REF", x] where x is ???
    - unique_hash (name of function + _f + number of call)
    - frame_id = number of call
    - is_zombie = assume false for now (TODO)
    - is_parent = assume false for now (TODO)
    - is_highlighted
    - parent_frame_id_list = ??
    - ordered_varnames = array of strings (local var names) in the order they show up